import { FormatUploadProgressPipe } from './format-upload-progress.pipe';

describe('FormatUploadProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatUploadProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
